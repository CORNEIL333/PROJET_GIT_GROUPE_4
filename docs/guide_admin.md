#📗 Guide Enseignant – Git Admin

##Générer un code enseignant (API)

```curl -X POST http://localhost:3000/api/admin/invite-code \
  -H "Authorization: Bearer VOTRE_TOKEN_ADMIN" \
  -H "Content-Type: application/json"```